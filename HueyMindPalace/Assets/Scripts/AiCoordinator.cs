using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AiCoordinator : MonoBehaviour
{
    // This class is the same as the Human Coordinator class, but does everything wihtout menu popups because 
    // it's the game ai handling the options.
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    public void StartCoordinator()
    {
        // This is run when character is asking what to do.
    }
}
